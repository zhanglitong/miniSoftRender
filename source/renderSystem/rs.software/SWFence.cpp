#include    "SWFence.h"
#include    "SWDevice.h"

namespace   FE
{
    SWFence::~SWFence()
    {
    }

    void    SWFence::reset()
    {
    }

    void    SWFence::wait(uint64 tm)
    {
    }

    bool    SWFence::create(const CreateInfo& cInfo)
    {
        return  true;
    }
}
